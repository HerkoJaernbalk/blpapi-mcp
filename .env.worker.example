# Worker process (Bloomberg-side)

# Worker bind host. Keep localhost unless you explicitly need private LAN access.
HOST=127.0.0.1

# Worker HTTP port (used with --http)
PORT=8080

# Set true only if you intentionally want HTTP mode inferred from env.
SSE_MODE=false
